export enum AdminFilter {
  Super = 'super',
  Regular = 'regular',
  None = ' default',
}

export enum DeliveriesFilter {
  NotStarted = 'not_started',
  OnTheWay = 'on_the_way',
  Delivered = 'delivered',
  Cancelled = 'cancelled',
  None = 'default',
}
